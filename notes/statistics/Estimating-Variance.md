The sample variance is actually an underestimate of the true variance.

This is because the sample variance is computed from the sample mean, which is as close as possible to all of the samples. Measuring the sample variance from the true mean will result in greater variance.

Therefore, we divide by $N-1$ instead of $N$ to get an unbiased estimate of the variance.


Last Reviewd 8/16/2026